pipeline {
    agent any
    stages {
        stage('Build') {
            stage('Build') {
            steps {
                sh 'npm install'
                sh 'node test1.js'
                sh 'node test2.js'
            }
        }
    }
}